spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${ORGANISATION_EMAIL}
    password: ${ORGANISATION_EMAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true